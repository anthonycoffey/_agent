# Bugsy — Project Context for Claude

## What Is This?

**Bugsy** is Anthony Coffey's personal AI agent stack, running 24/7 on a GCP Compute Engine VM.
The goal: automate the repetitive parts of freelance dev life — job hunting, lead gen, outreach,
research — and build a personal knowledge base that makes every AI interaction smarter over time.

Bugsy has a persona: 1970s New York Italian mobster. Calls Anthony "boss", "skip" like a mafia character in a movie. The persona wraps
useful output — it's flavor, not noise. Don't let it swallow the actual content.

**Anthony's experience level:** Newer to infra and agents. Explain concepts when something
unexpected happens, not just steps. He's a capable developer but GCP/Docker/n8n patterns
are still being internalized.

---

## Infrastructure

### VM

| Property | Value |
|---|---|
| Provider | GCP Compute Engine |
| Name | `agent-vm` |
| RAM | 16 GB |
| OS | Ubuntu |
| Managed by | Terraform (`tf/`) |
| Repo on VM | `~/bugsy/` (git clone of this repo) |
| App dir | `~/agent/` → symlink to `~/bugsy/agent/` |

Cloud-init bootstraps the VM on first boot only. Any changes after that require
`git pull` on the VM and manually re-importing n8n workflows (n8n stores its own
copy of each workflow in postgres — file changes don't auto-apply).

### Networking

- **No public IP.** VM is unreachable from the internet directly.
- **Cloudflare Tunnel** (`cloudflared` container) provides all public ingress. Outbound-only.
- **Tailscale** for SSH access from Anthony's machines. SSH as `agent@agent-vm`.
- Public subdomain: `n8n.coffey.codes` (via Cloudflare Tunnel → n8n container port 5678)

### Docker Services (`agent/docker-compose.yml`)

| Container | Image | Role |
|---|---|---|
| `agent-postgres` | pgvector/pgvector:pg16 | Primary DB for all app data + pgvector extension |
| `agent-qdrant` | qdrant/qdrant | Vector store for RAG (personal knowledge) |
| `agent-redis` | redis:7-alpine | General cache / queue |
| `agent-ollama` | ollama/ollama | Local LLM inference (no GPU currently) |
| `agent-litellm` | ghcr.io/berriai/litellm | Unified OpenAI-compatible API proxy |
| `agent-openwebui` | ghcr.io/open-webui/open-webui | Chat UI, port 3000 |
| `agent-n8n` | n8nio/n8n | Workflow automation, port 5678 |
| `agent-cloudflared` | cloudflare/cloudflared | Cloudflare Tunnel ingress |
| `agent-searxng` | searxng/searxng | Self-hosted metasearch for research workflows |

**Key n8n env vars:**
- `NODE_FUNCTION_ALLOW_EXTERNAL=axios` — required; Code nodes use axios for HTTP (native `fetch`
  and `$helpers.httpRequest` are not available in the n8n sandbox)

### Models Available (via LiteLLM at `http://litellm:4000/v1`)

| Model name | Backend |
|---|---|
| `claude-sonnet-4-6` | Anthropic |
| `claude-haiku-4-5` | Anthropic |
| `gpt-4o-mini` | OpenAI |
| `gemini-2.5-pro` | Google |
| `gemini-2.5-flash` | Google |
| `qwen3` | Ollama local (qwen3:8b) |
| `nomic-embed-text` | Ollama local (embeddings) |

---

## Repository Structure

```
_agent/
├── agent/                        # Everything deployed to ~/agent/ on the VM
│   ├── docker-compose.yml        # Full stack definition
│   ├── env.template              # .env reference (secrets generated by Terraform)
│   ├── rag-ingest.sh             # Shell script: POST markdown files → RAG ingest webhook
│   ├── rag/                      # Personal knowledge documents (gitignored: *.md)
│   │   ├── bio/                  # Resume, about-me, skills
│   │   ├── articles/             # Written/edited articles that capture Anthony's voice
│   │   ├── case-studies/         # Client work writeups
│   │   └── projects/             # Project descriptions
│   ├── litellm/config.yaml       # LiteLLM model list + settings
│   ├── n8n/workflows/            # n8n workflow JSON files
│   ├── postgres/
│   │   ├── init/                 # DB init scripts (run on first container start)
│   │   └── migrations/           # Schema migrations (run manually)
│   └── searxng/settings.yml      # SearXNG config
├── tf/                           # Terraform — GCP VM, networking, IAM, state backend
├── docs/                         # MkDocs Material — project documentation site (self-hosted)
├── logs/
│   ├── decisions-log.md          # Architecture decision log (newest first)
│   ├── incident-log.md           # Chronological record of deployment incidents + fixes
│   └── plans/                    # Working plans for in-flight initiatives
└── CLAUDE.md                     # This file
```

---

## n8n Workflows

All workflow JSON files live in `agent/n8n/workflows/`. After pushing changes, they must be
manually re-imported into n8n via the UI (Import from URL using the GitHub raw URL).

All workflows in the repo are intended to run in n8n (a couple are inactive until reviewed — see Active column). Per-workflow narrative + auto-generated node reference lives at `docs/workflows/<basename>.md`.

| File | Trigger | Purpose |
|---|---|---|
| `bugsy.json` | webhook `/slack-bugsy` (events) + `/ask` (slash) | Unified Slack assistant — DMs, @mentions, `/ask` slash. Memory (Postgres chat) + RAG (Qdrant) injection. |
| `bugsy-job-board-fetcher.json` | cron 5:30 CT M-F | Fetches 20+ job boards, batched haiku-4-5 fit-scoring (≥75 threshold), inserts to `job_listings`, Slack summary. |
| `bugsy-job-board-ui.json` | webhook GET `/job-board` | Dark-mode HTML board over `job_listings`. Default sort = best fit; min-score slider; row badges. |
| `bugsy-rag-ingest.json` | webhook POST `/rag-ingest` | Markdown → chunks → Ollama embed → Qdrant. Title fallback (frontmatter → H1 → filename). `onError: continueRegularOutput` on the embed step so a single chunk timeout doesn't kill the run. |
| `bugsy-rag-query.json` | webhook POST `/rag-query` | JSON-in/JSON-out RAG endpoint, no memory. `top_k` default 15. |
| `bugsy-leads-hunter.json` | cron 6:00 CT M-F | 12 SearXNG queries → dedupe → batched scoring → top 5 to `agent.leads` + Slack DM. |
| `bugsy-research.json` | webhook POST `/bugsy-research` (`/research <target>`) | On-demand prospect brief; SearXNG + homepage fetch → haiku-4-5 → DM the boss, optional Gmail draft. |
| `bugsy-inbox-watcher.json` | Gmail trigger | Classifies inbound mail (important / needs_reply / scheduling / promo), labels, drafts replies, pings Slack. Currently inactive in n8n. |
| `bugsy-jira-digest.json` | cron 8:00 + 16:00 CT M-F | Pulls work-email Jira notifications (`jira@ultrasoundai.atlassian.net`), summarizes activity, extracts action items, posts to `#mulberry-street` with ticket IDs linkified as `<URL\|UTT-NNN>` and link unfurl disabled. Uses Bitmotive Gmail credential. |

The earlier per-surface Slack flows (`bugsy-chat.json`, `bugsy-events.json`, `bugsy-slack-rag.json`) and `bugsy-whatsapp.json` were rolled into the unified workflow / removed; see `git log --diff-filter=D -- agent/n8n/workflows/` for their historical shapes.

---

## Database Schema (Postgres, `agent` DB)

### `job_listings`
Populated by the job board fetcher daily.

| Column | Type | Notes |
|---|---|---|
| `id` | SERIAL PK | |
| `source` | TEXT | Board name (e.g. `remotive`, `wwr-frontend`) |
| `title` | TEXT | |
| `company` | TEXT | |
| `url` | TEXT UNIQUE | Dedup key |
| `location` | TEXT | Always `'Remote'` currently |
| `salary_raw` | TEXT | Raw string from source |
| `tech_tags` | TEXT[] | Matched from STACK keyword list |
| `description` | TEXT | Stripped HTML, max 600 chars |
| `posted_at` | TIMESTAMPTZ | |
| `fetched_at` | TIMESTAMPTZ | Default now() |
| `status` | TEXT | `new` \| `reviewed` \| `dismissed` |
| `match_score` | INT | 0–100 LLM fit score (added migration 003). Only jobs ≥ threshold get inserted. |
| `match_reason` | TEXT | One-line LLM rationale for the score. |
| `scored_at` | TIMESTAMPTZ | When the score was computed. |

### `leads`
CRM table for outbound lead gen.

| Column | Type | Notes |
|---|---|---|
| `id` | SERIAL PK | |
| `domain` | TEXT UNIQUE | |
| `company` | TEXT | |
| `icp_bucket` | TEXT | ICP1–ICP4 |
| `score` | INT | 1–10 |
| `signals` | JSONB | Matched signal labels |
| `status` | TEXT | `new` \| `contacted` \| `replied` \| `dead` |
| `draft_email` | TEXT | LLM-generated outreach draft |

### `lead_events`
Audit trail for the leads CRM.

---

## Vector Store (Qdrant)

**Collection:** `personal_knowledge`
**Model:** `nomic-embed-text` (768-dimensional, Cosine similarity)
**Purpose:** RAG over Anthony's personal documents — resume, writing samples, case studies,
project history. Used as context injection for cover letter drafting, outreach writing, etc.

**Payload schema per point:**
```json
{
  "text": "chunk content",
  "title": "document title",
  "category": "bio | articles | case-studies | projects",
  "tags": ["tag1", "tag2"],
  "chunk_index": 0,
  "total_chunks": 5,
  "ingested_at": "ISO timestamp"
}
```

**Ingesting documents:**
1. Drop markdown files (with YAML frontmatter) into `~/agent/rag/<category>/`
2. Run `bash ~/agent/rag-ingest.sh` (or `bash ~/agent/rag-ingest.sh bio` for one category)
3. Files are POSTed to the RAG Ingest webhook and embedded automatically

**Frontmatter format:**
```markdown
---
title: My Resume
tags: [resume, react, node, typescript]
---

Content here...
```

---

## Key Architectural Decisions

- **No public IP** — Cloudflare Tunnel only. VM unreachable from internet directly.
- **`~/agent` is a git symlink** — cloud-init only runs once; git pull is the deploy mechanism.
- **GCE cloud-init requires plain-text user-data** — `gzip=false, base64_encode=false` in Terraform.
- **OS Login conflicts with cloud-init `users:` block** — use `bootcmd` to create users instead.
- **Docker bind mounts fail silently if source is missing** — missing source gets auto-created as
  a root-owned directory. Always commit the real file, or use `bind.create_host_path: false`.
- **n8n Code node sandbox has no HTTP primitives** — `fetch` and `$helpers.httpRequest` are
  unavailable. Use `axios` via `NODE_FUNCTION_ALLOW_EXTERNAL=axios`.
- **n8n `docker ps` showing `Up` ≠ healthy** — cross-check with `docker logs` and
  `docker inspect ... .State.RestartCount`.
- **Bind mounts are negotiated at container *create* time** — after fixing a host file/dir
  confusion, `docker compose restart` is not enough; use `docker compose rm -sf <svc> && up -d`.

---

## How to Deploy Changes

### Code / config changes
```bash
# On the VM
cd ~/bugsy && git pull
cd ~/agent && docker compose up -d <service>   # if docker-compose.yml changed
```

### n8n workflow changes
1. Push JSON changes to GitHub
2. In n8n UI → open workflow → ⋮ → Import from URL:
   `https://raw.githubusercontent.com/anthonycoffey/_agent/main/agent/n8n/workflows/<file>.json`
3. Save → activate

### Terraform changes
```powershell
# From Windows dev machine in tf/
terraform plan
terraform apply
# Force VM replacement if needed:
terraform apply "-replace=google_compute_instance.agent"
```

---

## Current State & Next Steps

### Working
- Full Docker stack deployed and healthy
- Job board: daily fetch from 20+ sources → LLM fit-score → postgres → web UI at `https://n8n.coffey.codes/webhook/job-board`
- RAG ingestion pipeline: markdown → chunk → embed → Qdrant

### In Progress / Next Up
- **Run migration 003 on the VM** to add scoring columns: `docker exec -i agent-postgres psql -U agent -d agent < ~/agent/postgres/migrations/003_job_listings_scoring.sql`
- **Re-import job board fetcher + UI workflows** in n8n after migration 003 lands
- **Pull `nomic-embed-text`** on Ollama if not already: `docker exec agent-ollama ollama pull nomic-embed-text`
- **Load personal knowledge docs** into `~/agent/rag/` (resume, articles, case studies)
- **Cover letter drafter** — webhook that takes a job listing ID, pulls the posting + RAG context, drafts a tailored cover letter
- **Job application tracker** — extend `job_listings` with `applied_at`, `interview_at`, `outcome`; auto follow-up reminders
- **Morning briefing** — 7am Slack digest: new overnight jobs, email replies, tech news via SearXNG
- **Lead gen pipeline** — flesh out `bugsy-leads-hunter.json`; research → personalized outreach → follow-up sequences

### Known Issues / Gotchas
- Many job board source sites have blocked the VM IP from scraping — job listings are stored
  but direct access to those sites from the VM may be blocked. Applying to jobs should be done
  from a personal machine, not routed through Bugsy.
- n8n workflow changes must be manually re-imported — there is no auto-sync between the repo
  and the running n8n instance.
- Job board UI status (reviewed/dismissed) is persisted in browser `localStorage` — it won't
  sync across devices.

---

## Commit Style

Conventional Commits. No `Co-Authored-By` trailer. Examples:
- `feat(rag): add personal knowledge ingestion pipeline`
- `fix(job-board-ui): guard localStorage against sandboxed iframe`
- `chore(tf): update VM machine type`

---

## Documentation discipline (REQUIRED)

When you change behavior, ship the doc change in the same commit. The docs in `docs/` are the source
of truth for how Bugsy operates — they go stale fast if we don't keep them honest.

For every meaningful change, ask: **does this change something a future reader would have to
re-discover by reading the JSON?** If yes, update the relevant page:

| You changed... | Update... |
|---|---|
| A workflow's behavior, schedule, inputs, outputs | `docs/workflows/<workflow>.md` |
| Database schema (columns, indexes, tables) | `docs/reference/database.md` **and** the schema table in this file |
| Webhook path or webhook contract | `docs/reference/webhooks.md` |
| Env var, secret, or model config | `docs/reference/env-vars.md` |
| Container, network, or ingress topology | `docs/architecture/*.md` |
| A non-obvious tradeoff or path-not-taken | one row in `logs/decisions-log.md` |
| Production incident + fix | one entry in `logs/incident-log.md` |
| New "current state" or "next up" item | the **Current State & Next Steps** section above |

If a doc page doesn't exist for the area being changed, create it rather than burying notes
in CLAUDE.md. CLAUDE.md is for *project context* (what's deployed, how it's wired, gotchas Claude
needs to know to operate safely) — not user-facing reference material.

Verify before claiming done: when you finish a change, re-read the relevant doc page top-to-bottom
and confirm it still describes the system that exists.
